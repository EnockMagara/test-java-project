# Application properties with various configuration errors

# Invalid property format
app.name=Test Application
app.version=1.0.0
app.description=This is a test application with configuration errors

# Database configuration with invalid values
database.url=jdbc:mysql://localhost:3306/testdb
database.username=testuser
database.password=testpass
database.driver=com.mysql.jdbc.Driver

# Invalid port number
server.port=99999

# Invalid boolean value
app.debug=maybe

# Invalid number format
app.timeout=not_a_number

# Missing value
app.missing=

# Duplicate keys
app.duplicate=first_value
app.duplicate=second_value

# Invalid date format
app.startDate=invalid_date

# Invalid URL format
app.externalUrl=not_a_valid_url

# Invalid JSON in properties
app.config={"invalid": json, format}

# Invalid environment variable reference
app.secret=${MISSING_ENV_VAR}

# Invalid file path
app.logFile=/invalid/path/with/special/chars/\0

# Invalid encoding
app.encoding=INVALID_ENCODING

# Invalid timeout value
app.connectionTimeout=-1

# Invalid memory settings
app.maxMemory=invalid_memory_size

# Invalid thread pool size
app.threadPoolSize=not_a_number

# Invalid SSL configuration
app.ssl.enabled=maybe
app.ssl.keystore=invalid_keystore_path

# Invalid logging configuration
logging.level.root=INVALID_LEVEL
logging.pattern=invalid_pattern_without_placeholders

# Invalid cache configuration
cache.ttl=invalid_time_format
cache.maxSize=negative_number

# Invalid email configuration
email.smtp.port=not_a_port
email.smtp.auth=invalid_boolean
